#include "main.h"
#include <stdlib.h>
/**
 * _islower - this does what you see.
 *
 * Description: This func does what it does.
 *
 */
int _islower(int c)
{
	if (islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
